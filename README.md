# suyashsingh.website

Personal site — a scroll-driven 3D swarm running the VeriSwarm protocol, plus the
work behind it. Static: one HTML file, nine images, no build step and no framework.

Three.js (r128) is loaded from a CDN; fonts from Google Fonts. Everything else is inline.

Served by GitHub Pages at https://suyashsingh.website
